import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marks.financas',
  appName: 'Marks Finanças',
  webDir: 'dist/public'
};

export default config;
